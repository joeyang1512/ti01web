
const port = 8081;
const appName = 'ti01';

module.exports = {
  port: 8081,
  open: true,
  compress: true,
  openPage: '/page/index/index.html',
  // publicPath: `localhost:${port}//${appName || ''}`,
};
